# AI密钥配置模板
# 使用方式：
# 1. 复制本文件为 application-ai-secret.yml
# 2. 在 application-ai-secret.yml 中填写真实 api-key
# 3. application-ai-secret.yml 已被 .gitignore 忽略，不应提交到GitHub

ai:
  openai:
    api-key:
    base-url: https://api.deepseek.com
    model: deepseek-v4-flash
